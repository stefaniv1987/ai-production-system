apiVersion: v1
kind: Service
metadata:
  name: ai-production-system-service
spec:
  selector:
    app: ai-production-system
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
